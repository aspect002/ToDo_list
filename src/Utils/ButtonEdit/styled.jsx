import styled from "styled-components";
import { IconButton } from '@mui/material';


export const StyledButtonEdit = styled(IconButton)`
    margin-left: auto;
  cursor: pointer;
  transition: background-color 0.3s ease;
  &:hover {
    color: #06f050;
  }
`
